// Syntax 
// condition ? expression1 : expression2;
// Where
// condition: is a boolean expression, which returns true false.
// expression1: executes if the condition is true.
// expression2: executes if the condition is false.

let num = -2;
let resul = num > 0 ? "positive" : "non-positive";
console.log(resul);

// // Conditional operator
// const isValid = false; //if true output is valid
// const message = isValid ? 'Valid' : 'Failed';
// console.log(message);

// let a=10
// let b=15
// let c= (a > b ? 'a is greater than b' : 'a is not greater than b');// true : false
// console.log(c)   //a is not greater than b 
 
//  let d = 10;
//  let e = 15;
//  let f:string;
//  if (d>e){
//     f='d is greater than e or true'
//  }else{
//     f='d is not greater than e or false'
//  }
// console.log(f);

//-----------------------------------
let isHungrey = false; //if true output is apple
let snack = isHungrey ? "apple" : "water";
console.log(`you should have some ${snack}.`);







